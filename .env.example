# GitHub fine-grained personal access token, scoped to this repo only,
# with "Issues: write" permission and nothing else (no Contents access).
GITHUB_TOKEN=

# owner/repo that api/contribute.ts opens issues against. Defaults to
# RyanTeigen/Autheo-Reading-List if unset.
GITHUB_REPO=RyanTeigen/Autheo-Reading-List
